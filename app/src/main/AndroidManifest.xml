<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="idv.chatea.gldemo">

    <uses-feature android:glEsVersion="0x00010000" android:required="true" />
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GLES1_Background_Activity"></activity>
        <activity android:name=".GLES1_Triangle_Activity"></activity>
        <activity android:name=".GLES1_ColorTriangle_Activity"></activity>
        <activity android:name=".GLES1_Projection_Activity"></activity>
    </application>

</manifest>
